apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: sentry-symbolicator
  name: sentry-symbolicator
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
