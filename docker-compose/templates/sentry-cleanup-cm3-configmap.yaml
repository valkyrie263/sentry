apiVersion: v1
data:
  .gitignore: |
    # Add all custom CAs in this folder
    *
    !.gitignore
kind: ConfigMap
metadata:
  labels:
    io.kompose.service: sentry-cleanup
  name: sentry-cleanup-cm3
